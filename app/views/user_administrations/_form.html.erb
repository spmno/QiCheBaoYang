<%= form_for(@user_administration) do |f| %>
  <% if @user_administration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_administration.errors.count, "error") %> prohibited this user_administration from being saved:</h2>

      <ul>
      <% @user_administration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
