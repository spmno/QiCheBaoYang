<%= form_for(@car_maintenance_info) do |f| %>
  <% if @car_maintenance_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_maintenance_info.errors.count, "error") %> prohibited this car_maintenance_info from being saved:</h2>

      <ul>
      <% @car_maintenance_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, class:"col-md-8" %>
  </div>
    <div class="clearfix"> </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, class:"col-md-8" %>
  </div>
    <div class="clearfix"> </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
