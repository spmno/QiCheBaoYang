<%= form_for(@auto_brand) do |f| %>
  <% if @auto_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auto_brand.errors.count, "error") %> prohibited this auto_brand from being saved:</h2>

      <ul>
      <% @auto_brand.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field ">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :auto_brand_photo %><br>
    <%= f.file_field :auto_brand_photo, class: 'input-group' %>
  </div>
  <div class="actions">
    <%= f.submit t('complete'), class: 'btn btn-primary' %>
  </div>
<% end %>
