<%= form_for(@maintenance_good) do |f| %>
  <% if @maintenance_good.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maintenance_good.errors.count, "error") %> prohibited this maintenance_good from being saved:</h2>

      <ul>
      <% @maintenance_good.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <%= f.text_field :photo %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
